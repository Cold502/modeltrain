# Markdown 中使用 LaTeX 数学公式教程

> 专为在 Markdown 文档（如笔记、博客、GitHub）中书写数学公式设计
> 
> 适用于：Obsidian、Typora、VS Code、GitHub、Jupyter Notebook 等支持 LaTeX 的 Markdown 编辑器

---

## 快速入门

### 你的例子
你想写的 **面积公式** 可以这样表达：

```markdown
## 圆的面积公式

行内公式：圆的面积 $A = \pi r^2$

独立公式（居中显示）：
$$
A = \pi r^2
$$
```

**渲染效果**：
- 行内：圆的面积 \( A = \pi r^2 \)
- 独立：
\[
A = \pi r^2
\]

---

## 1. 两种数学模式

### 1.1 行内公式（Inline Math）
用 `$...$` 包裹，公式嵌入文字中。

**语法**：
```markdown
爱因斯坦的质能方程是 $E = mc^2$，其中 $c$ 是光速。
```

**效果**：爱因斯坦的质能方程是 \( E = mc^2 \)，其中 \( c \) 是光速。

### 1.2 独立公式（Display Math）
用 `$$...$$` 包裹，公式单独成行，居中显示。

**语法**：
```markdown
牛顿第二定律：
$$
F = ma
$$
```

**效果**：
\[
F = ma
\]

---

## 2. 基础语法

### 2.1 上标和下标

| 效果 | 代码 | 说明 |
|------|------|------|
| \( x^2 \) | `$x^2$` | 上标 |
| \( x_i \) | `$x_i$` | 下标 |
| \( x^{2y} \) | `$x^{2y}$` | 多字符上标需要花括号 |
| \( x_{i,j} \) | `$x_{i,j}$` | 多字符下标 |
| \( x_i^2 \) | `$x_i^2$` | 同时使用上下标 |

**示例**：
```markdown
二次方程 $ax^2 + bx + c = 0$ 的解为：
$$
x_{1,2} = \frac{-b \pm \sqrt{b^2 - 4ac}}{2a}
$$
```

### 2.2 分数
用 `\frac{分子}{分母}` 表示。

```markdown
圆周率的定义：$\pi = \frac{C}{d}$

复杂分数：
$$
\frac{1}{1 + \frac{1}{2}}
$$
```

**效果**：
\[
\frac{1}{1 + \frac{1}{2}}
\]

### 2.3 根号
用 `\sqrt{...}` 或 `\sqrt[n]{...}`。

```markdown
- 平方根：$\sqrt{2} \approx 1.414$
- 立方根：$\sqrt[3]{27} = 3$
- 复杂根式：$\sqrt{x^2 + y^2}$
```

**效果**：\( \sqrt{x^2 + y^2} \)

### 2.4 希腊字母

| 小写 | 代码 | 大写 | 代码 |
|------|------|------|------|
| \( \alpha \) | `\alpha` | \( A \) | `A` |
| \( \beta \) | `\beta` | \( B \) | `B` |
| \( \gamma \) | `\gamma` | \( \Gamma \) | `\Gamma` |
| \( \delta \) | `\delta` | \( \Delta \) | `\Delta` |
| \( \theta \) | `\theta` | \( \Theta \) | `\Theta` |
| \( \pi \) | `\pi` | \( \Pi \) | `\Pi` |
| \( \sigma \) | `\sigma` | \( \Sigma \) | `\Sigma` |
| \( \omega \) | `\omega` | \( \Omega \) | `\Omega` |

**示例**：
```markdown
角速度公式：$\omega = \frac{\Delta \theta}{\Delta t}$
```

---

## 3. 常用符号

### 3.1 运算符

| 符号 | 代码 | 说明 |
|------|------|------|
| \( \times \) | `\times` | 乘号 |
| \( \div \) | `\div` | 除号 |
| \( \pm \) | `\pm` | 加减 |
| \( \mp \) | `\mp` | 减加 |
| \( \cdot \) | `\cdot` | 点乘 |
| \( \neq \) | `\neq` | 不等于 |
| \( \leq \) | `\leq` | 小于等于 |
| \( \geq \) | `\geq` | 大于等于 |
| \( \approx \) | `\approx` | 约等于 |
| \( \equiv \) | `\equiv` | 恒等于 |

### 3.2 微积分符号

```markdown
- 导数：$\frac{dy}{dx}$ 或 $f'(x)$
- 偏导数：$\frac{\partial f}{\partial x}$
- 积分：$\int_a^b f(x) \, dx$
- 双重积分：$\iint_D f(x,y) \, dA$
- 极限：$\lim_{x \to \infty} f(x)$
- 求和：$\sum_{i=1}^{n} x_i$
- 求积：$\prod_{i=1}^{n} x_i$
```

**效果示例**：
\[
\int_0^\infty e^{-x^2} \, dx = \frac{\sqrt{\pi}}{2}
\]

### 3.3 逻辑与集合

| 符号 | 代码 | 说明 |
|------|------|------|
| \( \forall \) | `\forall` | 任意 |
| \( \exists \) | `\exists` | 存在 |
| \( \in \) | `\in` | 属于 |
| \( \notin \) | `\notin` | 不属于 |
| \( \subset \) | `\subset` | 子集 |
| \( \cup \) | `\cup` | 并集 |
| \( \cap \) | `\cap` | 交集 |
| \( \emptyset \) | `\emptyset` | 空集 |
| \( \infty \) | `\infty` | 无穷 |

---

## 4. 高级技巧

### 4.1 多行公式（对齐）

使用 `\begin{aligned}` 环境，用 `&` 对齐，`\\` 换行。

```markdown
$$
\begin{aligned}
  (a + b)^2 &= a^2 + 2ab + b^2 \\
  &= a^2 + b^2 + 2ab
\end{aligned}
$$
```

**效果**：
\[
\begin{aligned}
  (a + b)^2 &= a^2 + 2ab + b^2 \\
  &= a^2 + b^2 + 2ab
\end{aligned}
\]

### 4.2 分段函数

使用 `\begin{cases}` 环境。

```markdown
$$
f(x) = \begin{cases}
  x^2, & x \geq 0 \\
  -x, & x < 0
\end{cases}
$$
```

**效果**：
\[
f(x) = \begin{cases}
  x^2, & x \geq 0 \\
  -x, & x < 0
\end{cases}
\]

### 4.3 矩阵

使用 `\begin{pmatrix}` 或其他矩阵环境。

```markdown
$$
A = \begin{pmatrix}
  a & b \\
  c & d
\end{pmatrix}
$$
```

**矩阵类型**：
- `pmatrix`：圆括号 \( \begin{pmatrix} a & b \\ c & d \end{pmatrix} \)
- `bmatrix`：方括号 \( \begin{bmatrix} a & b \\ c & d \end{bmatrix} \)
- `vmatrix`：行列式 \( \begin{vmatrix} a & b \\ c & d \end{vmatrix} \)
- `Bmatrix`：大括号 \( \begin{Bmatrix} a & b \\ c & d \end{Bmatrix} \)

### 4.4 上下标的扩展

```markdown
- 求和上下限：$\sum_{i=1}^{n} i = \frac{n(n+1)}{2}$
- 积分上下限：$\int_0^1 x^2 \, dx = \frac{1}{3}$
- 极限：$\lim_{x \to 0} \frac{\sin x}{x} = 1$
- 向量：$\vec{v} = \langle x, y, z \rangle$
- 带帽符号：$\hat{x}, \bar{x}, \tilde{x}$
```

### 4.5 大型运算符

在独立公式中，运算符会自动变大：

```markdown
行内：$\sum_{i=1}^{n} i$（小）

独立：
$$
\sum_{i=1}^{n} i
$$
（大）
```

**强制大型**：用 `\displaystyle`
```markdown
行内也变大：$\displaystyle \sum_{i=1}^{n} i$
```

---

## 5. 实战示例

### 5.1 微积分基本定理

```markdown
$$
\int_a^b f(x) \, dx = F(b) - F(a)
$$

其中 $F'(x) = f(x)$。
```

### 5.2 泰勒级数

```markdown
$$
e^x = \sum_{n=0}^{\infty} \frac{x^n}{n!} = 1 + x + \frac{x^2}{2!} + \frac{x^3}{3!} + \cdots
$$
```

### 5.3 欧拉公式

```markdown
$$
e^{i\theta} = \cos\theta + i\sin\theta
$$

特别地，当 $\theta = \pi$ 时：
$$
e^{i\pi} + 1 = 0
$$
```

### 5.4 二次方程求解

```markdown
对于方程 $ax^2 + bx + c = 0$，解为：

$$
x = \frac{-b \pm \sqrt{b^2 - 4ac}}{2a}
$$

判别式 $\Delta = b^2 - 4ac$ 决定根的性质：
- $\Delta > 0$：两个不同实根
- $\Delta = 0$：一个重根
- $\Delta < 0$：两个共轭复根
```

### 5.5 球体体积与表面积

```markdown
半径为 $r$ 的球：

- **体积**：$V = \frac{4}{3}\pi r^3$
- **表面积**：$A = 4\pi r^2$

注意：表面积是体积对半径的导数！
$$
\frac{dV}{dr} = 4\pi r^2 = A
$$
```

---

## 6. 常见问题与技巧

### 6.1 空格
LaTeX 会自动处理空格，但有时需要手动调整：

| 代码 | 效果 | 说明 |
|------|------|------|
| `a b` | \( ab \) | 无空格 |
| `a\ b` | \( a\ b \) | 小空格 |
| `a\quad b` | \( a\quad b \) | 中等空格 |
| `a\qquad b` | \( a\qquad b \) | 大空格 |

**积分中的空格**：
```markdown
正确：$\int f(x) \, dx$
错误：$\int f(x) dx$
```

### 6.2 多行公式编号

如果需要编号（部分编辑器支持）：

```markdown
$$
\begin{equation}
E = mc^2
\end{equation}
$$
```

### 6.3 文字与公式混合

在公式中插入文字，用 `\text{...}`：

```markdown
$$
f(x) = \begin{cases}
  1, & \text{当 } x > 0 \\
  0, & \text{当 } x \leq 0
\end{cases}
$$
```

### 6.4 括号自适应大小

用 `\left` 和 `\right` 让括号自动调整大小：

```markdown
错误：$(\frac{a}{b})$

正确：$\left(\frac{a}{b}\right)$
```

**效果对比**：
- 不自适应：\( (\frac{a}{b}) \)
- 自适应：\( \left(\frac{a}{b}\right) \)

---

## 7. 快速参考表

### 7.1 常用函数

| 函数 | 代码 |
|------|------|
| \( \sin x \) | `\sin x` |
| \( \cos x \) | `\cos x` |
| \( \tan x \) | `\tan x` |
| \( \ln x \) | `\ln x` |
| \( \log x \) | `\log x` |
| \( \exp(x) \) | `\exp(x)` |
| \( \lim \) | `\lim` |
| \( \max \) | `\max` |
| \( \min \) | `\min` |

### 7.2 箭头

| 符号 | 代码 |
|------|------|
| \( \rightarrow \) | `\rightarrow` |
| \( \leftarrow \) | `\leftarrow` |
| \( \Rightarrow \) | `\Rightarrow` |
| \( \Leftarrow \) | `\Leftarrow` |
| \( \Leftrightarrow \) | `\Leftrightarrow` |

### 7.3 特殊字体

| 效果 | 代码 | 说明 |
|------|------|------|
| \( \mathbb{R} \) | `\mathbb{R}` | 实数集 |
| \( \mathbb{Z} \) | `\mathbb{Z}` | 整数集 |
| \( \mathbb{N} \) | `\mathbb{N}` | 自然数集 |
| \( \mathbb{C} \) | `\mathbb{C}` | 复数集 |
| \( \mathcal{L} \) | `\mathcal{L}` | 花体字 |
| \( \mathbf{v} \) | `\mathbf{v}` | 粗体（向量）|

---

## 8. 你的例子完整版

```markdown
## 圆的几何性质

### 面积公式
圆的面积 $A$ 与半径 $r$ 的关系为：
$$
A = \pi r^2
$$

### 周长公式
圆的周长 $C$ 为：
$$
C = 2\pi r
$$

### 微积分视角
面积对半径的导数等于周长：
$$
\frac{dA}{dr} = \frac{d}{dr}(\pi r^2) = 2\pi r = C
$$

这说明当半径增加 $dr$ 时，新增的面积近似为一个宽度为 $dr$、长度为 $C$ 的薄环！
```

---

## 9. 不同编辑器的支持情况

| 编辑器 | 行内公式 | 独立公式 | 备注 |
|--------|----------|----------|------|
| Typora | `$...$` | `$$...$$` | 实时渲染 |
| Obsidian | `$...$` | `$$...$$` | 需启用 MathJax |
| VS Code | `$...$` | `$$...$$` | 需安装 Markdown+Math 插件 |
| GitHub | `$...$` | `$$...$$` | 原生支持 |
| Jupyter | `$...$` | `$$...$$` | Markdown 单元格 |

---

## 10. 练习题

试着用 LaTeX 写出以下公式：

1. **勾股定理**：直角三角形两直角边的平方和等于斜边的平方
2. **二项式定理**：\((a+b)^n\) 的展开式
3. **正态分布**：概率密度函数
4. **麦克斯韦方程组**：四个方程（挑战！）

<details>
<summary>点击查看答案</summary>

```markdown
1. 勾股定理：
$$
a^2 + b^2 = c^2
$$

2. 二项式定理：
$$
(a+b)^n = \sum_{k=0}^{n} \binom{n}{k} a^{n-k} b^k
$$

3. 正态分布：
$$
f(x) = \frac{1}{\sigma\sqrt{2\pi}} e^{-\frac{(x-\mu)^2}{2\sigma^2}}
$$

4. 麦克斯韦方程组：
$$
\begin{aligned}
  \nabla \cdot \vec{E} &= \frac{\rho}{\varepsilon_0} \\
  \nabla \cdot \vec{B} &= 0 \\
  \nabla \times \vec{E} &= -\frac{\partial \vec{B}}{\partial t} \\
  \nabla \times \vec{B} &= \mu_0 \vec{J} + \mu_0\varepsilon_0 \frac{\partial \vec{E}}{\partial t}
\end{aligned}
$$
```

</details>

---

## 11. 进阶资源

- **在线 LaTeX 编辑器**：[Overleaf](https://www.overleaf.com/)
- **公式识别工具**：[Mathpix](https://mathpix.com/) - 截图转 LaTeX
- **符号查询**：[Detexify](http://detexify.kirelabs.org/) - 手写识别
- **完整参考**：[LaTeX Math Symbols](https://www.cmor-faculty.rice.edu/~heinken/latex/symbols.pdf)

---

## 总结

**三步法**：
1. **行内**用 `$...$`，**独立**用 `$$...$$`
2. **上标** `^`，**下标** `_`，**分数** `\frac{}{}`
3. 记住常用希腊字母和符号，其他随用随查

**口诀**：先想"我要表达什么"，再查"对应的 LaTeX 命令"。多练几次就熟练了！

祝你的数学笔记越写越漂亮！📐✨
